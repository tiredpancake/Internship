"use client";
import React from "react";
import Link from "next/link";

const AuthRedirect = ({ text, linkText, linkHref }) => {
  return (
    <div className="mt-5 text-t-gray text-md">
      {text}{" "}
      <Link href={linkHref} className="text-t-purple font-semibold">
        {linkText}
      </Link>
    </div>
  );
};

export default AuthRedirect;
